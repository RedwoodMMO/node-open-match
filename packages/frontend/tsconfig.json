{
  "extends": "../../tsconfig-base.json",
  "compilerOptions": {
    "outDir": "lib" /* Redirect output structure to the directory. */,
    "rootDir": "src"
  },
  "include": ["./src/**/*.ts"],
  "references": [
    { "path": "../api" }
  ]
}
